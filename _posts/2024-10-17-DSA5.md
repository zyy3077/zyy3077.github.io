---
layout: post
title: "2024fall-数算-第五章-二叉树-题解"
date: 2024-10-17
categories: [DSA, 2024fall]
tags: [academic, blog]
---

<style>
    /* 在这里定义你的样式 */
    body {
        background-color: #f0f0f0;  /* 背景颜色 */
        color: dimgray;             /* 字体颜色 */
        font-family: 'Cayman', sans-serif; /* 字体样式 */
        margin: 0;                /* 页边距 */
    }
    @media (min-width: 1200px) {
        body {
            margin: 20%; /* 大屏幕 */
        }
    }
    @media (max-width: 1199px) {
        body {
            margin: 10%; /* 中屏幕 */
        }
    }
    h1 {
        font-size: 2em;            /* 标题字体大小 */
    }
    p {
        font-size: 1.2em;            /* 段落字体大小 */
    }

    code, pre { 
    font-family: Consolas, Monaco, 'Andale Mono', 'Ubuntu Mono', monospace;
    font-size: 14px;
    line-height: 2; /* Adjust line height to avoid overlapping */
    white-space: pre-wrap; /* Wrap lines to avoid horizontal scrolling */
    word-wrap: break-word; /* Break long words to fit within the container */
    max-height: 600px;         /* 设置最大高度 */
    
    overflow: auto; /* Add scrollbars when needed */
    padding: 10px; /* Add padding for better readability */
    background-color: #f5f5f5; /* Light background color for contrast */
    
    border-radius: 5px; /* Rounded corners for aesthetics */
  }
</style>


# 第五章 二叉树
题目详见：[http://dsa.openjudge.cn/2024dsachapter5/](http://dsa.openjudge.cn/2024dsachapter5/)

## Huffman编码树
构造一个具有n个外部节点的扩充二叉树，每个外部节点Ki有一个Wi对应，作为该外部节点的权。使得这个扩充二叉树的叶节点带权外部路径长度总和最小：

                                     Min( W1 * L1 + W2 * L2 + W3 * L3 + … + Wn * Ln)

	 
Wi:每个节点的权值。<br>
Li:根节点到第i个外部叶子节点的距离。<br>
编程计算最小外部路径长度总和。


输入<br>
第一行输入一个整数t，代表测试数据的组数。<br>
对于每组测试数据，第一行输入一个整数n，外部节点的个数。第二行输入n个整数，代表各个外部节点的权值。<br>
2<=n<=100

输出<br>
输出最小外部路径长度总和。

样例输入<br>
2<br>
3<br>
1 2 3<br>
4<br>
1 1 3 5

样例输出<br>
9<br>
17

### 题解
构造Huffman二叉树的过程：每次选两个值最小的节点合并，合并后节点的值为二者之和，叶子节点到根节点距离*叶子节点值之和为带权外部路径长度总和。<br>
用stl中的优先队列模拟最小堆，每次合并pop前两个元素并push二者之和，累加pop出的元素值即可，因为在第n层的叶子节点自身被累加或者它与另一个值的和作为节点被累加，一共n次，即累加了它的带权路径长度。<br>
priority_queue的基本用法如下:

                                     std::priority_queue<int> max_pq; // 默认是最大优先队列
                                     std::priority_queue<int, std::vector<int>, std::greater<int>> min_pq; // 最小优先队列


### 代码实现
```cpp
#include<iostream>
#include<queue>
#include<vector>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--) {
        int n, ans = 0;
        cin >> n;
        priority_queue<int,vector<int>, greater<int>>q;
        for(int i = 0; i < n; i ++) {
            int x;
            cin >> x;
            q.push(x);
        }
        while(q.size() != 1)
        {
            int t1,t2;
            t1 = q.top();q.pop();
            t2 = q.top();q.pop();
            ans += t1 + t2; //以自身or和的形式被累加n次，即乘以它的深度n
            q.push(t1 + t2);
        }	
        cout << ans << endl;
    }
	return 0;
}
```

## Pre-Post-erous!
We are all familiar with pre-order, in-order and post-order traversals of binary trees. A common problem in data structure classes is to find the pre-order traversal of a binary tree when given the in-order and post-order traversals. Alternatively, you can find the post-order traversal when given the in-order and pre-order. However, in general you cannot determine the in-order traversal of a tree when given its pre-order and post-order traversals. Consider the four binary trees below:

							    a   a   a   a
							   /   /     \   \
							  b   b       b   b
							 /     \     /     \
							c       c   c       c

All of these trees have the same pre-order and post-order traversals. This phenomenon is not restricted to binary trees, but holds for general m-ary trees as well.

输入<br>
Input will consist of multiple problem instances. Each instance will consist of a line of the form
m s1 s2
indicating that the trees are m-ary trees, s1 is the pre-order traversal and s2 is the post-order traversal.All traversal strings will consist of lowercase alphabetic characters. For all input instances, 1 <= m <= 20 and the length of s1 and s2 will be between 1 and 26 inclusive. If the length of s1 is k (which is the same as the length of s2, of course), the first k letters of the alphabet will be used in the strings. An input line of 0 will terminate the input.

输出<br>
For each problem instance, you should output one line containing the number of possible trees which would result in the pre-order and post-order traversals for the instance. All output values will be within the range of a 32-bit signed integer. For each problem instance, you are guaranteed that there is at least one tree with the given pre-order and post-order traversals.

样例输入<br>
2 abc cba<br>
2 abc bca<br>
10 abc bca<br>
13 abejkcfghid jkebfghicda<br>
0

样例输出<br>
4<br>
1<br>
45<br>
207352860

### 题解
题目的大致意思是给定先序和后序遍历，要求输出可能的m叉树的数目。先看先序序列，得出二叉树的根节点（第一个元素），再看第二个元素，是第一个子树（最多有m个子树，这里我最开始直接当作二叉树的左子树处理了，得到了错误的结果）的根节点；而后序序列中左子树的根节点的位置即为左子树结束的位置，即先序序列的第二个元素在后序序列中的位置及其之前的部分为第一个子树；在这之后的序列里重复上述找子树根节点的过程，即可找到所有子树，记数目为k。每棵子树内部，可用递归求解可能的情况树，而根节点与子树之间，有C_m k中排列的可能（m个分叉中选k个分叉作为子树不为空的分叉）。故实现思路为递归与组合计数结合。<br>
要另外注意的是，如果用阶乘求解组合数，会涉及整型溢出的问题，很难发现，所以最好使用递推数组的方法，借助组合数的数学性质：

							C_m k = C_m-1 k-1 + C_m-1 k

       
此外，因为用多了string类型导致忘了char*如何初始化和输入：

							char s1[N];  scanf("%s", s1);

       
strlen,strcat等函数接受的是char*类型的参数。

### 代码实现
```cpp
#include<iostream>
#include<cstring>
using namespace std;

int Comb[21][21];
void calComb()
{
    Comb[0][0] = 1;
    for(int i = 1; i <= 20; ++i)
    {
        Comb[i][0] = 1;
        Comb[i][i] = 1;
        for(int j = 1; j < i; ++j)
        {
            Comb[i][j] = Comb[i-1][j-1] + Comb[i-1][j];
        }
    }
}

int count(char* s1, char* s2, int len, int m) {
    int i = 1;
    int j = 0;
    int branch = 0;
    int answer = 1;
    while (i < len) {
        for (; j < len; j ++) {
            if (s1[i] == s2[j]) {
                branch ++;
                answer *= count(s1+i, s2+i-1, j-i+2, m);
                i = j+2;
                break;
            }
        }
    }
    answer *= Comb[m][branch];
    return answer;
}

int main()
{
    int m;
    calComb();
    while (cin >> m && m != 0) {
        char s1[27], s2[27];
        scanf("%s", s1);
        scanf("%s", s2);
        cout << count(s1, s2, strlen(s1), m) << endl;
    }
	return 0;
}
```
